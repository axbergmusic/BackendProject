Hello Nahid. To run the script please chmod the script (chmod u+x ./script.sh) and then proceeed to type ./script.sh to run the script.
it will perform a mvn clean compile followed by a mvn exec:java which will run our project. Then simply just follow the menu to test our skin project. Thanks
